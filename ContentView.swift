import SwiftUI

struct ContentView: View {
    var body: some View {
        HStack {
            Text("NAME PICKER")
                .font(.largeTitle)
                .bold()
            Text("PRO")
                .background(.red)
                .font(.largeTitle)
                .bold()
                
        }
    }
}
